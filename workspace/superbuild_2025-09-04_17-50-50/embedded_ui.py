import tkinter as tk\nfrom tkinter import ttk\n\nclass EmbeddedUI:\n    def __init__(self, llm_system):\n        self.llm_system = llm_system\n        \n        self.root = tk.Tk()\n        self.root.title("Multi-Agent LLM System")\n        \n        self.create_widgets()\n    \n    def create_widgets(self):\n        main_frame = ttk.Frame(self.root, padding="10")\n        main_frame.grid(row=0, column=0, sticky=(tk.W, tk.E, tk.N, tk.S))\n        \n        ttk.Label(main_frame, text="Query:").grid(row=0, column=0, sticky=tk.W)\n        \n        self.query_entry = ttk.Entry(main_frame, width=50)\n        self.query_entry.grid(row=0, column=1, padx=5, pady=5, sticky=(tk.W, tk.E))\n        \n        self.submit_button = ttk.Button(main_frame, text="Submit", command=self.submit_query)\n        self.submit_button.grid(row=0, column=2, padx=5, pady=5)\n        \n        self.response_text = tk.Text(main_frame, width=80, height=20)\n        self.response_text.grid(row=1, column=0, columnspan=3, padx=5, pady=5)\n        \n        self.root.columnconfigure(0, weight=1)\n        self.root.rowconfigure(0, weight=1)\n        main_frame.columnconfigure(1, weight=1)\n        \n    def submit_query(self):\n        query = self.query_entry.get()\n        response = self.llm_system.process_query(query)\n        self.response_text.delete(1.0, tk.END)\n        self.response_text.insert(tk.END, response)\n    \n    def run(self):\n        self.root.mainloop()\n