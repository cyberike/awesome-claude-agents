import os\n\nclass DevOpsEngineer:\n    def __init__(self, name):\n        self.name = name\n    \n    def receive_task(self, task):\n        self.task = task\n    \n    def build(self):\n        # Implement build logic here\n        print(f"{self.name} is building the project...")\n        # Execute build commands, e.g., running shell scripts, Makefiles, etc.\n        os.system("make build")\n    \n    def test(self):\n        # Implement test logic here  \n        print(f"{self.name} is running tests...")  \n        # Execute test commands, e.g., running unit tests, integration tests, etc.\n        os.system("make test")\n\n    def deploy(self):\n        # Implement deployment logic here\n        print(f"{self.name} is deploying the application...")\n        # Execute deployment commands, e.g., pushing to production, updating services, etc.\n        os.system("make deploy")\n\n    def execute_task(self):\n        print(f"{self.name} received task: {self.task}")\n        self.build()\n        self.test()  \n        self.deploy()\n        print(f"{self.name} completed the task successfully!")\n