import tensorflow as tfdef quantize_model(model_path, quantized_model_path):    # Load the saved model    model = tf.keras.models.load_model(model_path)        # Convert the model to TensorFlow Lite format    converter = tf.lite.TFLiteConverter.from_keras_model(model)    converter.optimizations = [tf.lite.Optimize.DEFAULT]        # Quantize the model    quantized_model = converter.convert()        # Save the quantized model    with open(quantized_model_path, 'wb') as f:        f.write(quantized_model)if __name__ == '__main__':    model_path = 'path/to/saved/model.h5'    quantized_model_path = 'path/to/quantized/model.tflite'    quantize_model(model_path, quantized_model_path)    print(f'Model quantized and saved at: {quantized_model_path}')