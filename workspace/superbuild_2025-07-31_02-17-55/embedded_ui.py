import tkinter as tk\nfrom tkinter import ttk\n\nclass EmbeddedUI(tk.Tk):\n    def __init__(self):\n        super().__init__()\n\n        self.title("Embedded UI")\n        self.geometry("800x480")\n\n        self.create_widgets()\n\n    def create_widgets(self):\n        # Create main container\n        main_frame = ttk.Frame(self, padding="10")\n        main_frame.grid(row=0, column=0, sticky=(tk.W, tk.E, tk.N, tk.S))\n        \n        # Create labels\n        title_label = ttk.Label(main_frame, text="Welcome to Embedded UI", font=("Arial", 24))\n        title_label.grid(row=0, column=0, columnspan=2, pady=(0, 20))\n\n        info_label = ttk.Label(main_frame, text="This is an example of an embedded user interface.", font=("Arial", 16))\n        info_label.grid(row=1, column=0, columnspan=2)\n\n        # Create buttons\n        button1 = ttk.Button(main_frame, text="Button 1")\n        button1.grid(row=2, column=0, padx=(0, 10), pady=20)\n\n        button2 = ttk.Button(main_frame, text="Button 2")\n        button2.grid(row=2, column=1, pady=20)\n\n        # Configure grid weights\n        self.columnconfigure(0, weight=1)\n        self.rowconfigure(0, weight=1)\n        main_frame.columnconfigure(0, weight=1)\n        main_frame.columnconfigure(1, weight=1)\n\nif __name__ == "__main__":\n    app = EmbeddedUI()\n    app.mainloop()\n