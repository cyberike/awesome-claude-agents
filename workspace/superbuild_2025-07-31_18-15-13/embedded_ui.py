import tkinter as tk\nfrom tkinter import ttk\n\nclass EmbeddedUI:\n    def __init__(self, llm_system):\n        self.llm_system = llm_system\n        \n        self.root = tk.Tk()\n        self.root.title("Multi-Agent LLM System")\n        \n        self.create_widgets()\n    \n    def create_widgets(self):\n        main_frame = ttk.Frame(self.root, padding="10")\n        main_frame.grid(row=0, column=0, sticky=(tk.W, tk.E, tk.N, tk.S))\n        \n        ttk.Label(main_frame, text="User Input:").grid(row=0, column=0, sticky=tk.W)\n        \n        self.user_input = tk.Text(main_frame, height=5, width=50)\n        self.user_input.grid(row=1, column=0, sticky=(tk.W, tk.E))\n        \n        submit_button = ttk.Button(main_frame, text="Submit", command=self.submit_input)\n        submit_button.grid(row=2, column=0, sticky=tk.E)\n        \n        ttk.Label(main_frame, text="System Output:").grid(row=3, column=0, sticky=tk.W)\n        \n        self.system_output = tk.Text(main_frame, height=10, width=50, state=tk.DISABLED)\n        self.system_output.grid(row=4, column=0, sticky=(tk.W, tk.E))\n        \n        self.root.columnconfigure(0, weight=1)\n        self.root.rowconfigure(0, weight=1)\n        main_frame.columnconfigure(0, weight=1)\n        main_frame.rowconfigure(1, weight=1)\n        main_frame.rowconfigure(4, weight=1)\n    \n    def submit_input(self):\n        user_input = self.user_input.get("1.0", tk.END).strip()\n        response = self.llm_system.process_input(user_input)\n        \n        self.system_output.configure(state=tk.NORMAL)\n        self.system_output.delete("1.0", tk.END)\n        self.system_output.insert(tk.END, response)\n        self.system_output.configure(state=tk.DISABLED)\n    \n    def run(self):\n        self.root.mainloop()\n