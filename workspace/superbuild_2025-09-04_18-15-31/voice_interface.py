import speech_recognition as sr\nimport pyttsx3\n\nclass VoiceInterface:\n    def __init__(self):\n        self.recognizer = sr.Recognizer()\n        self.engine = pyttsx3.init()\n\n    def listen(self):\n        with sr.Microphone() as source:\n            print("Listening...")\n            self.recognizer.pause_threshold = 1\n            self.recognizer.adjust_for_ambient_noise(source, duration=1)\n            audio = self.recognizer.listen(source)\n\n        try:\n            print("Recognizing...")\n            query = self.recognizer.recognize_google(audio, language='en-US')\n            print(f"User said: {query}")\n        except sr.UnknownValueError:\n            print("Sorry, I did not understand that.")\n            query = ""\n        except sr.RequestError:\n            print("Sorry, my speech service is currently down.")\n            query = ""\n\n        return query\n\n    def speak(self, text):\n        self.engine.say(text)\n        self.engine.runAndWait()\n\nif __name__ == "__main__":\n    voice_interface = VoiceInterface()\n    \n    while True:\n        query = voice_interface.listen().lower()\n        \n        if 'hello' in query:\n            voice_interface.speak("Hello! How can I assist you today?")\n        elif 'bye' in query:\n            voice_interface.speak("Goodbye!")\n            break\n        else:\n            voice_interface.speak("Sorry, I don't have a response for that yet.")\n